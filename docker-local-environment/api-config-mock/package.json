
{
  "name": "api-config-mock",
  "version": "1.0.0",
  "description": "Mock server for API Config IBAN delete",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 3000"
  },
  "keywords": [
    "mock",
    "json-server"
  ],
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}